namespace Exercicio10
{
    class Circulo
    {
        // Para o cálcular a área de um círculo é preciso saber o valor do raio
        // A = pi * (raio * raio)
        public double raio;
    }

    class Esfera
    {
        // Para o cálcular o volume de uma esfera é preciso saber o valor do raio
        // V = (4/3) * pi * (raio * raio * raio)
        public double raio;
    }
    internal class Program
    {
        static void Main(string[] args)
        {
            //Console.WriteLine("Hello, World!");
        }
    }
}
